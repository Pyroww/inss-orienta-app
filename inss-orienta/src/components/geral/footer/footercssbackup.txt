/* NAVEGAÇÃO INFERIOR */
.bottom-nav {
  position: fixed; /* Mantido o seu, apenas limpei a duplicata de position */
  bottom: 0;
  left: 0;
  width: 100%;
  height: 60px;
  background-color: var(--nav-bg);
  border-top: 1px solid var(--border-color);
  display: flex;
  justify-content: space-around;
  align-items: center;
  z-index: 1000;
  padding-bottom: calc(10px + env(safe-area-inset-bottom));
}

.nav-item {
  background: none;
  border: none;
  display: flex;
  flex-direction: column;
  align-items: center;
  color: var(--nav-gray);
  font-size: 12px;
  cursor: pointer;
  width: 33%;
  position: relative; /* <-- CRUCIAL PARA A PÍLULA FLUTUANTE */

  /* 1. O EXORCISMO DO QUADRADO AZUL */
  -webkit-tap-highlight-color: transparent;
  user-select: none;
  outline: none;

  /* Transição mestre de tempo */
  transition: color 0.25s ease, transform 0.15s ease;
}

/* O texto do botão */
.nav-item span {
  margin-top: 4px;
  transition: transform 0.2s ease, font-weight 0.2s ease;
}

/* ALVO MÁGICO: O Ícone dentro do botão (assumindo que o react-icons gera um <svg>) */
.nav-item svg {
  font-size: 20px;
  transition: transform 0.3s cubic-bezier(0.34, 1.56, 0.64, 1); /* Efeito Mola (Desaceleração elástica) */
}

/* 2. A GRAVIDADE: O que acontece no exato milissegundo em que o polegar afunda na tela */
.nav-item:active {
  transform: scale(0.92);
  opacity: 0.8;
}

/* 3. A RECOMPENSA: O estado de selecionado */
.nav-item.active {
  color: var(--nav-active);
  font-weight: bold;
}

/* O ícone dá um salto de 25% no tamanho e sobe 2 pixels */
.nav-item.active svg {
  transform: scale(1.25) translateY(-2px);
}

/* O texto desce 1 pixel para abrir espaço para o salto do ícone */
.nav-item.active span {
  transform: translateY(1px);
}

/* A PÍLULA MODERNA DE FUNDO (Estilo Google Super Apps) */
.nav-item::before {
  content: '';
  position: absolute;
  top: 4px; /* Fica perfeitamente centralizada atrás do ícone */
  width: 46px;
  height: 28px;
  background-color: var(--nav-active);
  opacity: 0;
  border-radius: 14px;
  transform: scaleX(0.4);
  transition: all 0.3s cubic-bezier(0.175, 0.885, 0.32, 1.275);
  z-index: -1; /* Garante que fique atrás do ícone e do texto */
}

/* Quando ativado, a pílula ganha 15% de cor e estica horizontalmente */
.nav-item.active::before {
  opacity: 0.15; 
  transform: scaleX(1);
}

/* REGRA DE ACESSIBILIDADE DO ALTO CONTRASTE INTOCADA */
.app-container.high-contrast .nav-item.active span {
  text-decoration: underline;
  text-underline-offset: 4px;
}