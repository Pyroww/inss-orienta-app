/* =========================================================
   CALENDÁRIO DE PAGAMENTO (PADRÃO WALLET PREMIUM / ACESSSÍVEL)
   ========================================================= */

.calendario-container {
  display: flex;
  flex-direction: column;
  height: 100%;
  background-color: var(--bg-color);
  margin: -15px; 
}

/* O HEADER SECUNDÁRIO FIXADO CONFORME SUA CORREÇÃO MILITAR */
.calendario-container .interno-header {
  position: fixed !important;
  top: 60px !important; /* Colado exatamente abaixo do cabeçalho principal */
  left: 0 !important;
  width: 100vw !important;
  display: flex !important;
  align-items: center !important;
  background-color: var(--inss-blue) !important;
  color: white !important;
  padding: 5px 15px !important;
  gap: 16px !important;
  box-shadow: 0 4px 10px rgba(0, 0, 0, 0.15) !important;
  z-index: 999 !important;
  box-sizing: border-box !important;
}

.calendario-container .interno-header h2 {
  margin: 0 !important;
  font-size: 1.2rem !important;
  font-weight: 600 !important;
  letter-spacing: -0.3px !important;
}

/* CONTEÚDO SCROLL (Com o empurrão seguro para o cabeçalho fixo) */
.calendario-conteudo {
  flex: 1; 
  overflow-y: auto; 
  display: flex;
  flex-direction: column;
  padding: 16px;
  padding-top: 75px; /* Abre espaço exato para o cabeçalho fixo não cobrir o banner */
  padding-bottom: 95px; /* Impede o rodapé de cobrir a calculadora */
  scroll-behavior: smooth;
  overscroll-behavior-y: contain;
}

/* BANNER PRINCIPAL (Sangria e cantos arredondados modernos) */
.banner-area {
  width: 100%;
  height: 180px;
  background-color: #f1f5f9;
  border-radius: 16px; /* Bordas mais suaves estilo Squircle */
  overflow: hidden;
  margin-bottom: 24px;
  box-shadow: 0 6px 16px rgba(0, 0, 0, 0.04);
  border: 1px solid var(--border-color);
}

.banner-area img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  display: block;
}

/* TEXTOS EDUTORIAIS E INSTRUÇÕES */
.titulo-secao {
  color: var(--title-color);
  font-size: 1.35rem;
  font-weight: 800;
  letter-spacing: -0.5px;
  margin: 0 0 16px 2px;
}

.passo-instrucao {
  margin-bottom: 24px;
  padding: 0 2px;
}

.passo-instrucao h4 {
  color: var(--title-color);
  font-size: 1.08rem;
  font-weight: 750;
  letter-spacing: -0.2px;
  margin: 0 0 8px 0;
  line-height: 1.3;
}

.passo-instrucao p {
  color: var(--text-color);
  font-size: 0.95rem;
  line-height: 1.55;
  margin: 0;
  opacity: 0.88;
}

/* O destaque em vermelho exigido para o idoso achar o dígito */
.passo-instrucao p strong {
  color: #ef4444; /* Vermelho carmim vivo de alta acessibilidade */
  background-color: rgba(239, 68, 68, 0.08);
  padding: 2px 6px;
  border-radius: 6px;
  font-weight: 800;
}

/* MAQUETE DO CARTÃO DE CRÉDITO/BENEFÍCIO (Efeito Premium) */
.cartao-ilustracao {
  width: 100%;
  height: 170px;
  background: linear-gradient(135deg, var(--card-bgType2), var(--bg-color));
  border-radius: 20px;
  margin-top: 16px;
  display: flex;
  align-items: center;
  justify-content: center;
  position: relative;
  overflow: hidden;
  border: 1.5px dashed var(--border-color);
  box-shadow: inset 0 4px 12px rgba(0,0,0,0.01);
}

.icone-cartao-bg {
  position: absolute;
  font-size: 130px;
  color: var(--inss-blue);
  opacity: 0.06; /* Flutua elegantemente no fundo */
  z-index: 1;
}

.cartao-ilustracao img {
  width: 90%;
  height: 90%;
  object-fit: contain;
  z-index: 2;
  position: absolute;
  border-radius: 12px;
  filter: drop-shadow(0 8px 20px rgba(0,0,0,0.08));
}

/* =========================================
   A PAINEL DA CALCULADORA / SIMULADOR
   ========================================= */
.calculadora-box {
  background-color: var(--card-bg);
  border: 1px solid var(--border-color);
  border-radius: 24px; /* Casulo arredondado moderníssimo */
  padding: 22px 18px;
  margin-bottom: 24px;
  box-shadow: 0 8px 25px rgba(0, 0, 0, 0.03);
}

.calc-header {
  display: flex;
  align-items: center;
  gap: 10px;
  margin-bottom: 20px;
  padding-bottom: 12px;
  border-bottom: 1px solid var(--border-color);
}

.icone-calc {
  color: var(--inss-blue);
  font-size: 22px;
  filter: drop-shadow(0 2px 4px rgba(24, 88, 160, 0.2));
}

.calc-header h4 {
  margin: 0;
  font-size: 1.15rem;
  font-weight: 800;
  color: var(--title-color);
  letter-spacing: -0.3px;
}

.form-group {
  display: flex;
  flex-direction: column;
  margin-bottom: 18px;
}

.mt-2 {
  margin-top: 12px;
}

.form-group label {
  font-size: 0.9rem;
  color: var(--title-color);
  margin-bottom: 10px;
  font-weight: 750;
}

/* OS BOTÕES DE RÁDIO (Transformados em Cards Horizontais) */
.radio-group {
  display: flex;
  flex-direction: column;
  gap: 10px;
  background-color: transparent;
  padding: 0;
  border: none;
}

.radio-label {
  display: flex;
  align-items: center;
  gap: 12px;
  font-size: 0.95rem;
  font-weight: 600;
  color: var(--text-color);
  cursor: pointer;
  background-color: var(--card-bgType2);
  padding: 14px 16px;
  border-radius: 14px;
  border: 1.5px solid var(--border-color);
  -webkit-tap-highlight-color: transparent;
  transition: all 0.2s ease;
}

/* Feedback visual quando o rádio estiver marcado por dentro */
.radio-label:has(input:checked) {
  border-color: var(--inss-blue);
  background-color: rgba(24, 88, 160, 0.06);
  color: var(--inss-blue);
  font-weight: 750;
}

/* Estilização moderna da bolinha do Radio nativo */
.radio-label input[type="radio"] {
  accent-color: var(--inss-blue); /* Pinta a bolinha com o azul do INSS no Android/iOS */
  width: 18px;
  height: 18px;
  cursor: pointer;
}

/* INPUT TEXTO DO BENEFÍCIO (Cápsula de Digitação) */
.form-group input[type="text"] {
  padding: 14px 16px;
  border: 1.5px solid var(--border-color);
  border-radius: 14px;
  font-size: 1.05rem;
  background-color: var(--bg-color);
  color: var(--text-color);
  outline: none;
  box-sizing: border-box;
  transition: border-color 0.2s ease;
}

.form-group input[type="text"]:focus {
  border-color: var(--inss-blue);
}

.form-group input[type="text"]::placeholder {
  opacity: 0.45;
}

/* BOTÃO DE CONSULTAR (Física Elástica da Apple) */
.btn-consultar {
  width: 100%;
  background: linear-gradient(135deg, var(--inss-blue), #1d4ed8);
  color: white;
  border: none;
  padding: 16px;
  border-radius: 16px;
  font-size: 1rem;
  font-weight: 800;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 10px;
  cursor: pointer;
  margin-top: 8px;
  box-shadow: 0 4px 14px rgba(24, 88, 160, 0.25);
  -webkit-tap-highlight-color: transparent;
  transition: transform 0.1s ease, box-shadow 0.1s ease;
}

.btn-consultar:active {
  transform: scale(0.97);
  box-shadow: 0 2px 6px rgba(24, 88, 160, 0.1);
}

/* =========================================
   ÁREA DE RESULTADOS DINÂMICOS
   ========================================= */
.resultado-alerta {
  margin-top: 20px;
  padding: 18px;
  border-radius: 18px;
  display: flex;
  flex-direction: column;
  align-items: center;
  text-align: center;
  gap: 14px;
  box-sizing: border-box;
  width: 100%;
  animation: resultadoSurgir 0.3s cubic-bezier(0.16, 1, 0.3, 1) forwards;
}

/* CARD ERRO (Alerta Carmim Macio) */
.resultado-alerta.erro {
  background: linear-gradient(135deg, rgba(239, 68, 68, 0.12), rgba(220, 38, 38, 0.04));
  border: 1.5px solid rgba(239, 68, 68, 0.3);
  color: var(--text-color);
  flex-direction: row; /* Mantém o padrão alinhado à esquerda do seu JSX */
  text-align: left;
  align-items: flex-start;
}

.resultado-alerta.erro p {
  color: #dc2626;
  font-weight: 600;
}

.res-icone {
  font-size: 24px;
  color: #ef4444;
  flex-shrink: 0;
  margin-top: 2px;
}

/* CARD SUCESSO (Status Esmeralda de Alto Nível) */
.resultado-alerta.sucesso {
  background: linear-gradient(145deg, var(--card-bgType2), var(--card-bg));
  border: 2px solid #10b981; /* Verde esmeralda de sucesso */
  color: var(--text-color);
  box-shadow: 0 10px 25px rgba(16, 185, 129, 0.08);
}

/* O PLACAR GIGANTE DO DÍGITO */
.digito-destaque {
  display: flex;
  flex-direction: column;
  align-items: center;
  background-color: var(--bg-color);
  padding: 12px 24px;
  border-radius: 16px;
  border: 1px solid var(--border-color);
  width: 80%;
  box-sizing: border-box;
}

.digito-destaque span {
  font-size: 0.85rem;
  font-weight: 700;
  color: var(--text-color);
  opacity: 0.6;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  margin-bottom: 4px;
}

.digito-destaque strong {
  font-size: 3.8rem; /* Número gigante para o idoso enxergar sem esforço */
  line-height: 1;
  color: #10b981; /* Acende o número em verde esmeralda vibrante */
  font-weight: 900;
  letter-spacing: -1px;
}

.resultado-alerta p {
  margin: 0;
  font-size: 0.96rem;
  line-height: 1.55;
  font-weight: 500;
  opacity: 0.95;
}

/* =========================================
   DÚVIDA COMUM (CARD DE INFORMAÇÃO)
   ========================================= */
.duvida-comum {
  background: linear-gradient(135deg, rgba(245, 158, 11, 0.1), rgba(245, 158, 11, 0.03));
  border: 1px solid rgba(245, 158, 11, 0.25);
  border-radius: 16px;
  padding: 16px;
  margin-top: 8px;
}

.duvida-header {
  display: flex;
  align-items: center;
  gap: 10px;
  margin-bottom: 8px;
}

.icone-duvida {
  color: #f59e0b; /* Amarelo Ouro de Atenção */
  font-size: 20px;
}

.calendario-container .duvida-header h4 {
  margin: 0;
  color: #d97706; /* Tom âmbar escuro legível */
  font-size: 1.05rem;
  font-weight: 750;
  letter-spacing: -0.2px;
}

.duvida-comum p {
  margin: 0;
  color: var(--text-color);
  font-size: 0.92rem;
  line-height: 1.5;
  font-weight: 500;
  opacity: 0.9;
}

@keyframes resultadoSurgir {
  from { opacity: 0; transform: translateY(12px) scale(0.98); }
  to { opacity: 1; transform: translateY(0) scale(1); }
}